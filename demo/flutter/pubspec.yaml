name: marketplace_frontend
description: Flutter Web for Marketplace
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  web_socket_channel: ^3.0.1
  flutter_bloc: ^8.1.6
  graphql_flutter: ^5.3.0
  # For SSR, use flutter_ssr or similar, but Flutter Web SSR is limited; use server for SEO

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true